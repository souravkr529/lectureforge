#python src/main.py
# API Keys - Configure at least one
# Priority 1: Claude (if available, will be used first in auto mode)
CLAUDE_API_KEY=

# Priority 2: OpenAI (fallback if Claude is not configured, required for TTS)
OPENAI_API_KEY=

# Model Configuration
CLAUDE_MODEL=claude-sonnet-4-6
OPENAI_MODEL=gpt-4.1

# TTS Configuration (requires OpenAI API key)
TTS_MODEL=tts-1
VOICE=alloy